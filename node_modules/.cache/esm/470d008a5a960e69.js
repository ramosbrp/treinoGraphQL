let createServer;_5a9‍.w("http",[["createServer",["createServer"],function(v){createServer=v}]]);

const server = createServer((req, res) => {
    switch (req.url) {
        case '/status': {
            res.writeHead(200);
            res.write('Okay');
            res.end();
            break;
        }
        default: {
            res.writeHead(404, 'Service not found');
            res.write('Okay');
            res.end();
        }
           
    }
});

const PORT = process.env.PORT ? parseInt(process.env.PORT) : 8000;
const HOSTNAME = process.env.HOSTNAME || '172.0.0.1';

server.listen(8000, HOSTNAME, () => {
    console.log(`Server is listening at ${ HOSTNAME }:${ PORT }.`);
});